export class GridMeta {
  // ORIGIN From which to make adjustments of each placement into the svg viewport
  X_ORIGIN: number;
  Y_ORIGIN: number;
  // Dimensions of a single coordinate point of the cartesian grid represented by the svg viewport
  COORDINATE_WIDTH: number;
  COORDINATE_HEIGHT: number;
}
